name: DEV - Vulnerability Scan & Auto-merge

on:
  push:
    branches: [ "dev" ]

permissions:
  contents: write
  pull-requests: write

jobs:
  vulnerability-scan-and-merge:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: 📦 Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔍 Run AI Vulnerability Scanner
        id: scan
        run: |
          echo "🔍 Scanning code for vulnerabilities..."
          python scripts/ai_scan.py

      - name: ✅ Scan Passed - Merge to TEST
        if: success()
        run: |
          echo "✅ No vulnerabilities detected!"
          echo "🚀 Merging to TEST branch..."
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Fetch and checkout test branch
          git fetch origin test
          git checkout test
          git pull origin test
          
          # Merge dev into test
          git merge origin/dev --no-ff -m "🔀 Auto-merge from dev (vulnerability scan passed)"
          
          # Push to test - this will trigger the TEST workflow
          git push origin test

      - name: ❌ Scan Failed - Vulnerabilities Detected
        if: failure()
        run: |
          echo "❌ VULNERABILITIES DETECTED!"
          echo "🚫 Merge to TEST blocked"
          echo "Please fix the vulnerabilities before proceeding"
          exit 1
